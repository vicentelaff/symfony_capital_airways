<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid col-11">
		<a class="navbar-brand ms-4" href="/">Capital Airways</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse justify-content-end me-4" id="navbarNavDropdown">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" href="{{ path("flight_index") }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path("flight_new") }}">Nouveau Vol</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Aide</a>
				</li>
				{% if is_granted("ROLE_USER") %}
					<li class="nav-item">
						{# <span class="mr-5">Bonjour
							{{ app.user.firstName }}, vous êtes connecté
						</span> #}
						<a href="{{ path("app_logout") }}" class="nav-link">Logout</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
